version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Install Project
          command: npm install
      - run:
          name: Front End Unit Tests
          command: ng test
      - run:
          name: Print the Current Time
          command: date
